fastapi
uvicorn[standard]
# asyncua 2.0 is the first release whose serializer works natively on Python
# 3.14; 1.x needed a local ua_binary monkey patch (removed in this baseline).
asyncua>=2.0.1,<3
python-multipart
mcp~=2.0.0
watchfiles
tree-sitter
tree-sitter-typescript
zeroconf>=0.150.0
httpx>=0.28.1
PyNaCl>=1.6.2
# Generates the manager's self-signed certificate when an operator turns on
# HTTPS. Already present transitively via asyncua; pinned here as a direct
# dependency because core.tls_settings imports it.
cryptography>=50.0.0
